
  # Nexera POS

  This is a code bundle for Nexera POS. The original project is available at https://www.figma.com/design/MuIah9MksoEarjgqcyOznO/Nexera-POS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  